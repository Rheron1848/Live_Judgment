# AGENTS.md — 通用 AI 协作规范

> 与工具无关的 AI agent 行为基线，适用于 Kimi Code、Claude Code、Codex 等任何会读取 `AGENTS.md` 的编码 agent。
>
> **用法**：把本文件复制到新项目根目录，按项目情况增删。项目专属内容（技术栈、命令、目录结构）写进项目自己的文档，本文件只放通用基线并用指针引用——不要在多个文件里重复同一套规则，避免真源漂移。
>
> **权威头（复制到项目后保留并填写）**：本文件复制自模板仓库 `Rheron1848/agent-template`，同步时模板 commit：`9bf6c21`；如有出入以模板仓库权威版为准。项目定制规则**不要写进上方正文**，追加到文末「项目定制」小节或单独文件，便于上游更新时整体替换正文。在项目里验证过的通用经验可回写模板仓库，流程见其 README「分发与定制」。

## 1. 铁律（违反会出事的硬边界）

1. 未经用户明确要求，不执行 `git commit / push / 建分支 / reset / rebase` 等任何 git 变更；需要时先说明再经同意。
2. 危险操作先确认：删除文件、批量移动、重置状态、访问生产环境、全局安装/卸载依赖。
3. 最小改动：只做完成目标所需的最小修改；不顺手重构、不清理无关代码、不动无关格式。
4. 工作区已有用户未提交的修改时，保留并绕开，绝不覆盖。
5. 写入共享资产（设计稿、线上数据、他人维护的文档）只在副本/工作区进行，正式产物由人合并。

## 2. 以事实为准

- **先读后写**：修改前先读相关代码与文档，不凭记忆判断进度和现状。
- 交接文档、聊天记录可能过期：以源码、git 历史、实际运行结果交叉核验。
- 会话开始先做只读健康检查：确认环境、依赖、服务连接正常再动手。
- **验证命令固化**：在项目的 README/AGENTS 中写清 lint / typecheck / build / test 命令；改动后运行对应命令并亲眼看输出，不声称"应该没问题"。

## 3. QA 分级（按改动影响范围执行，不做无谓全量）

动手前先定级，写进 todo/计划；「完成」= 该级别必做项全绿，不是「所有检查都跑过」。

| 级别 | 典型改动 | 必做 | 跳过 |
|------|---------|------|------|
| **L0 微改** | 文案、注释、不影响行为的单文件改动 | 类型检查（如涉及） | 其余全部 |
| **L1 局部** | 单组件/单页面/单函数 | 类型检查 + lint + 目视确认受影响范围 | build、重型验证脚本 |
| **L2 模块** | 跨模块链路、路由、共享数据结构、明显视觉改动 | L1 全套 + 相关模块的验证脚本 + 一次构建 | 无关验证、全量截图 |
| **L3 交付** | 架构调整、构建/导出链路、发版前 | 全部检查 + 全量验证 | — |

- **升级触发器**：改动构建脚本、共享类型、被多处引用的模块 → 至少 L2。
- **降级保护**：跳过的检查事后暴露问题 → 记录归因，不回退到"每次都全量"。
- **视觉产物必须亲眼看**：截图等视觉产物要用图像读取能力逐张核对，脚本绿灯不算视觉验收。

## 4. 工作流程：规格 → 计划 → 执行 → 评审

- **QA 定级 L2 及以上**的改动先写**规格（spec）**：列出「已拍板决策」与「明确不做」清单，防止范围蔓延；L0/L1 小改不需要规格。
- **计划（plan）** 拆成可独立执行的任务；每个任务有明确输入（涉及文件/接口/步骤）和验收标准。
- 逐任务执行、逐任务评审、留台账：任务 → 提交区间 → 评审结论 → 是否可合并。
- 执行中偏离计划是允许的，但必须在报告中**显式申报偏差及理由**。
- 验证设计用**先失败后通过**：先确认检查能抓住问题（红），再实现到通过（绿）。
- **臃肿自查**：工作中留意臃肿信号——单文件过长、重复代码、死代码/死导出、职责不清；发现即记入报告的 Concerns。清理不作为顺手的事：先列出清单请用户确认范围，再作为**独立改动**执行，不借清理之名做无关重构。

## 5. 冲突与决策权

- **用户是唯一拍板人**。发现文档之间、文档与代码之间矛盾时：不擅自二选一，如实列出矛盾与候选口径，请用户裁决。
- **AI 的自主边界**：只能自主决定不改变用户可见结果的实现细节（重构、类型设计、缓存、测试组织等）；凡影响行为、接口、视觉的决策，先问。
- **单一可信源**：每类信息指定唯一权威文件；副本注明"以权威版为准"；冲突时按项目文档预先写明的裁决链处理，未写明裁决链的按上一条上报用户裁决。

## 6. 交付与沟通

- 用与用户相同的语言回应（默认中文）；文档语言与项目现有文档一致。
- **注释与文档语言约定**：
  - 代码注释：**中英双语**（先中后英），保持简洁，注释说明"为什么"而非复述代码。
  - 工程文档（设计、规格、计划、报告等）：中文即可。
  - README 等门面文档：**先中文、后英文**双语（同一文件内中文全文在前，英文全文在后）。
- 交付附**人工检查清单**：去哪看、预期看到什么、重点风险点。
- 报告固定结构：做了什么 / 如何验证 / 改了哪些文件 / 自审发现 / 需要人知悉的顾虑。
- 决策与历史档案**不可删**；规则被覆盖时显式写出"旧规则 → 新规则"。
- 把踩过的坑沉淀为文档（What Worked / What Didn't），让后来的人与 agent 不重走弯路。

## 7. 稳健性

- 外部依赖（路径、服务、凭据）缺失时**优雅降级**：说明降级了什么、如何恢复，不报错中断整个任务。
- 多机/异步协作场景下，用仓库本身当传输层（提交即同步）。

## 8. 流程增强：superpowers（环境提供时必须主动调用）

如果当前环境安装了 superpowers（或提供同名 skills 的等价插件），**必须主动调用其流程 skill，不要徒手执行**：

- 新功能 / 行为改动：先 `brainstorming` 探索意图与设计，获批后再动手
- 多步实现：`writing-plans` 产出计划，`subagent-driven-development` 逐任务执行与评审
- 遇到 bug / 测试失败 / 异常行为：先 `systematic-debugging` 定位根因再修
- 声称完成前：`verification-before-completion`，先跑验证拿到证据再下结论

模板仓库 `templates/` 的简报 / 报告 / 规格 / 计划模板作为上述流程的**留档格式**；环境没有 superpowers 时，按模板手动执行同一流程，产出同样的留档。模板资产见上游仓库 <https://github.com/Rheron1848/agent-template>（本项目未复制，按需取用）。

## 项目定制（复制到项目后在此追加，不影响上游同步）

- **项目形态**：B 站直播弹幕治理工具，产物为 Tampermonkey / Violentmonkey 油猴脚本。
- **技术栈**：Bun + Vite + TypeScript（构建为单文件 userscript）。
- **验证命令**：`bun run typecheck`（tsc --noEmit）、`bun run lint`（biome check）、`bun run build`（tsc + vite build，产物 `dist/live-judgment.user.js`）。
- **需求权威文档**：`docs/requirements.md`。
- **本地约束**：仓库根可能存在 `AGENTS.local.md`（已通过 `.git/info/exclude` 排除，永不入库）；存在时必读，其中的约束优先于一切提效考虑。
